#include <stdio.h>

/**
 * main - Program entry point
 *
 * Return: Always 0.
 */
int main(void)
{
	/* Expands to the name of the current input file as string */
	printf("%s\n", __FILE__);
	return (0);
}
